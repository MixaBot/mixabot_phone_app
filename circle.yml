version: 2
jobs:
   build:
     docker:
       - image: circleci/node:6.10.3
     steps:
       - checkout
       - run: npm install
       - run: npm run build:prod
       - run: sudo /opt/google-cloud-sdk/bin/gcloud compute copy-files ./www clay-bar:/app/
